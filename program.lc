let `zero` = \fx.x
let `succ` = \nfx.f(nfx)
let `plus` = \mnfx.mf(nfx)
let `mult` = \mnf.m(nf)
let `exp` = \mn.nm
let `pred` = \nfx.n(\gh.h(gf))(\u.x)(\u.u)
let `minus` = \mn.n`pred`m

let `one` = `succ` `zero`

`one`

let `true` = \xy.x
let `false` = \xy.y

let `isZero` = \n.n(\x.`false`)`true`
